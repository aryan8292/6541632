{
  "name": "pricetrackerpro",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit --checkJs --allowJs --target es2022 --module esnext --moduleResolution bundler src/**/*.js",
    "lint": "eslint src",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "eslint": "^9.10.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}